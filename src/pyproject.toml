[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "webpage-saver"
version = "0.0.1"
dependencies  = ["asyncio", "aiohttp", "playwright", "beautifulsoup4", "ua-generator"]
